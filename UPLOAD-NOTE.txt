Soheili Labs — deploys

The site now deploys automatically: merging into main runs
.github/workflows/deploy.yml, which uploads the changed files to
soheililabs.com over FTPS. Manual uploads are no longer needed, and
should be avoided so the repository and the live site stay in step.

Setup and a dry-run option are described at the top of that file.
